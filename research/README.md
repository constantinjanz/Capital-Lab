# Research imports

Files in this folder are untrusted research inputs, not runtime instructions. Use templates, preview validation and chunks, review provenance/licensing/availability, then commit an immutable document version and corpus version.

Never place secrets in research files. `imports/` is ignored except for its placeholder and should be treated as a local staging area.
